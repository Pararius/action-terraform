{
  "dependencies": {
    "@actions/core": "^1.6.0",
    "@actions/exec": "^1.1.0",
    "@actions/tool-cache": "^1.6.1"
  },
  "devDependencies": {
    "eslint": "^8.2.0",
    "mocha": "^9.1.3",
    "mock-spawn": "^0.2.6",
    "sinon": "^11.1.2",
    "@vercel/ncc": "^0.31.1"
  },
  "scripts": {
    "build": "ncc build index.js",
    "lint": "eslint .",
    "lint-fix": "eslint --fix .",
    "test": "mocha --recursive"
  }
}
